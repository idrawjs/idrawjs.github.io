{
  "scripts": {
    "docs:dev": "vuepress dev src",
    "docs:build": "vuepress build src",
    "dev": "npm run docs:dev",
    "build": "npm run docs:build && node ./scripts/after-build.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/idrawjs/idrawjs.github.io.git"
  },
  "license": "MIT",
  "devDependencies": {
    "vuepress": "^1.8.2"
  }
}
